# 인수인계 — 스캔 결의서 파일명 변경 프로그램

> 새 채팅에서 이어가기 위한 문서. **이 파일은 커밋하지 않았다.**

## 지금 상태

브레인스토밍과 계획 수립이 끝났고, **구현은 한 줄도 시작하지 않았다.**

```
2278b52  설계서 추가
364b23b  구현 계획 추가
```

작업 트리는 이 `HANDOFF.md`만 추적되지 않은 상태로 남아 있다.

## 새 채팅에서 할 일

구현 계획을 서브에이전트 방식으로 실행하면 된다. Task 1부터 6까지 순서대로다.

```
/superpowers:subagent-driven-development docs/superpowers/plans/2026-09-07-scan-organizer.md 실행
```

계획 파일에 각 태스크의 전체 코드와 테스트가 들어 있으므로, 새 채팅은 계획 파일만 읽으면 된다.

## 읽어야 할 문서

| 파일 | 내용 |
|---|---|
| `docs/superpowers/specs/2026-09-07-scan-organizer-design.md` | 설계서 — 무엇을 왜 이렇게 만드는가 |
| `docs/superpowers/plans/2026-09-07-scan-organizer.md` | 구현 계획 — 태스크 6개, 전체 코드 포함 |
| `/Users/cheonjamin/Downloads/스캔결의서_자동정리_개발제안서_v1.md` | 원본 제안서 (참고용, 범위가 크게 줄었음) |

---

## 대화에서 확정된 것 (문서에 없는 맥락)

원본 제안서는 **무인 폴더 감시형**이었으나, 질의응답을 거쳐 **사용자 선택 + 사전 확인형**으로 바뀌었다. 그 과정에서 확정된 사항:

1. **결의번호 형식** — `YYYYMMDD-` + 일련번호. 일련번호는 **7자리 또는 4자리** 두 가지뿐.
   예: `20260315-0000005`, `20260414-0001`
   - 대화 초반에 "6자리"라고 하셨다가 이후 "7자리"로 두 번 정정하셨다. **7/4가 최종**이다.
   - 5·6자리는 일부러 인식하지 않는다 (오인식 방지).

2. **라벨 혼재** — 문서에 `결의번호 :` 라벨이 붙는 경우와 단독으로 찍히는 경우가 섞여 있다.
   그래서 라벨 우선 → 실패 시 단독, 2단 폴백 구조다.

3. **보관·폴더 이동은 하지 않는다** — "보관은 내가 진행함". 파일명만 바꾸고 제자리에 둔다.
   제안서 4.3(폴더 규칙), FR-08, FR-09가 통째로 삭제된 이유다.

4. **데스크톱 앱 창** — 웹페이지·배치 실행·상시 감시를 검토했으나 최종적으로 tkinter 창을 선택.
   파일 선택 방식은 `filedialog`(탐색기 다중 선택), 드래그앤드롭 아님 (의존성 최소화).

5. **정렬은 파일 수정 시각 오름차순** = 스캔된 순서.
   단, **순서는 표시용일 뿐**이고 파일명은 각 PDF 안에 적힌 번호에서 나온다.
   (스캔 순서대로 번호를 새로 매기는 방식이 아니다 — 이 부분을 명시적으로 확인받았다.)

6. **실행 환경은 Windows 10/11**. 개발은 macOS.

---

## 주의할 것

### 실제 스캔 PDF 샘플이 아직 없다

정규식은 구술하신 형식에만 근거한다. 실제 스캐너의 OCR 텍스트가 어떻게 추출되는지 미확인이다.
설계서 12장과 계획 마지막 절에 이 내용과 대응 방법이 적혀 있다.

**Windows 실기 검수는 반드시 복사본 폴더에서 먼저 해야 한다.**

### 계획에서 특히 중요한 부분

- **Task 3의 `plan_renames`는 파일을 절대 건드리면 안 된다.** 계획과 실행이 분리되어야
  "바꾸기 전에 보여주고 승인받는" 화면이 성립한다. 이걸 검증하는 테스트가 Task 3 Step 5에 있다.
- 자기 자신을 "기존 파일"로 세어 `_2`를 붙이는 버그를 조심할 것 (테스트로 잡아뒀다).
- 로컬 환경 확인 완료: Python 3.13.9, tkinter 8.6, pypdf 6.14.0.

---

## 최종 산출물 (계획 완료 시)

```
scan_organizer.py        프로그램 전체
config.ini               정규식 2줄, 한글 주석
스캔정리_실행.bat         더블클릭 실행
README.md                한글 사용자 안내
test_scan_organizer.py   자동 테스트 24건
```
