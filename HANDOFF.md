# 인수인계 — 스캔 결의서 파일명 변경 프로그램

## 지금 상태

**구현 완료.** Task 1~6 전부 끝났고 자동 테스트 24건이 통과한다.
남은 것은 **Windows 실기 검수** 하나뿐이다.

```
5fec2b6  인수인계 문서 추가
e7a6f75  실행 배치파일과 한글 사용자 안내서 추가
353ade9  tkinter GUI 구현
247c49a  이름 변경 실행과 로그 기록 구현
268964d  이름 변경 계획 수립 구현
6909a08  PDF 첫 페이지 텍스트 읽기 구현
71aeb24  결의번호 추출 로직과 설정 파일 로딩 구현
```

## 산출물

| 파일 | 내용 |
|---|---|
| `scan_organizer.py` | 프로그램 전체 (추출 → PDF 읽기 → 계획 → 실행 → GUI) |
| `config.ini` | 정규식 2줄, 한글 주석 |
| `스캔정리_실행.bat` | 더블클릭 실행 (UTF-8) |
| `README.md` | 한글 사용자 안내 |
| `test_scan_organizer.py` | 자동 테스트 24건 |

```bash
python3 test_scan_organizer.py   # 24개 모두 통과
python3 scan_organizer.py        # 창 실행
```

## 다음 작업 — Windows 실기 검수

계획 파일 마지막 절의 T-01 ~ T-09를 실제 업무용 PC에서 수행한다.
**첫 실행은 반드시 복사본 폴더에서 한다.**

실제 스캔 PDF 샘플이 아직 없어서, 정규식은 구술하신 형식(`YYYYMMDD-` + 7자리 또는
4자리)에만 근거한다. 실제 스캐너의 OCR 텍스트가 어떻게 추출되는지는 미확인이다.

미인식이 나오면 `정리기록.log`의 `추출텍스트:` 뒤를 본다. 실제로 읽힌 글자가
그대로 나오므로, 거기 맞춰 `config.ini`의 정규식만 고치면 된다. **코드는 건드리지
않는다.** 특히 확인할 것: 라벨 표기(`결의번호`/`문서번호`/`지출결의번호`), 하이픈이
다른 문자로 추출되는지(`‐`, `–`, 공백), 표 안에 있을 때 셀 경계.

## 읽어야 할 문서

| 파일 | 내용 |
|---|---|
| `docs/superpowers/specs/2026-09-07-scan-organizer-design.md` | 설계서 — 무엇을 왜 이렇게 만드는가 |
| `docs/superpowers/plans/2026-09-07-scan-organizer.md` | 구현 계획 — 검수 항목 T-01~T-09 포함 |
| `/Users/cheonjamin/Downloads/스캔결의서_자동정리_개발제안서_v1.md` | 원본 제안서 (참고용, 범위가 크게 줄었음) |

---

## 대화에서 확정된 것 (문서에 없는 맥락)

원본 제안서는 **무인 폴더 감시형**이었으나, 질의응답을 거쳐 **사용자 선택 + 사전 확인형**으로 바뀌었다. 그 과정에서 확정된 사항:

1. **결의번호 형식** — `YYYYMMDD-` + 일련번호. 일련번호는 **7자리 또는 4자리** 두 가지뿐.
   예: `20260315-0000005`, `20260414-0001`
   - 대화 초반에 "6자리"라고 하셨다가 이후 "7자리"로 두 번 정정하셨다. **7/4가 최종**이다.
   - 5·6자리는 일부러 인식하지 않는다 (오인식 방지).

2. **라벨 혼재** — 문서에 `결의번호 :` 라벨이 붙는 경우와 단독으로 찍히는 경우가 섞여 있다.
   그래서 라벨 우선 → 실패 시 단독, 2단 폴백 구조다.

3. **보관·폴더 이동은 하지 않는다** — "보관은 내가 진행함". 파일명만 바꾸고 제자리에 둔다.
   제안서 4.3(폴더 규칙), FR-08, FR-09가 통째로 삭제된 이유다.

4. **데스크톱 앱 창** — 웹페이지·배치 실행·상시 감시를 검토했으나 최종적으로 tkinter 창을 선택.
   파일 선택 방식은 `filedialog`(탐색기 다중 선택), 드래그앤드롭 아님 (의존성 최소화).

5. **정렬은 파일 수정 시각 오름차순** = 스캔된 순서.
   단, **순서는 표시용일 뿐**이고 파일명은 각 PDF 안에 적힌 번호에서 나온다.
   (스캔 순서대로 번호를 새로 매기는 방식이 아니다 — 이 부분을 명시적으로 확인받았다.)

6. **실행 환경은 Windows 10/11**. 개발은 macOS.

---

## 구현하면서 알게 된 것

- 계획 파일 Task 2의 테스트 코드에 문법 오류가 있었다 (`b"한글"`은 파이썬에서
  불가). `.encode()`로 고쳤다. 계획 파일 자체는 수정하지 않았다.
- Task 5 Step 4(창을 눈으로 확인)는 macOS에서 화면 기록 권한이 없어 스크린샷을
  못 찍었다. 대신 위젯 구성과 Treeview 행 내용을 코드로 검증했고, 창을 실제로
  띄워 확인도 마쳤다.
- 로컬 환경: Python 3.13.9, tkinter 8.6, pypdf 6.14.0.

## 주의할 것

- **`plan_renames`는 파일을 절대 건드리지 않는다.** 계획과 실행이 분리되어야
  "바꾸기 전에 보여주고 승인받는" 화면이 성립한다. 이걸 검증하는 테스트가 있다.
- 자기 자신을 "기존 파일"로 세어 `_2`를 붙이는 버그는 테스트로 잡아뒀다.
